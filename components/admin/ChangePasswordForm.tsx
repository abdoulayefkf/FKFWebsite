"use client";
import { FormEvent, useEffect, useState } from "react";
import { useRouter } from "next/navigation";
export function ChangePasswordForm(){const router=useRouter();const[csrf,setCsrf]=useState("");const[message,setMessage]=useState("");useEffect(()=>{fetch("/api/auth/csrf").then(r=>r.json()).then(d=>setCsrf(d.csrfToken))},[]);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);if(f.get("newPassword")!==f.get("confirm"))return setMessage("New passwords do not match");const r=await fetch("/api/auth/change-password",{method:"POST",headers:{"content-type":"application/json","x-csrf-token":csrf},body:JSON.stringify({currentPassword:f.get("currentPassword"),newPassword:f.get("newPassword")})});const d=await r.json();if(!r.ok)return setMessage(d.error);router.push("/admin/users");router.refresh()}return <form onSubmit={submit} className="mx-auto mt-24 max-w-md space-y-5 rounded-2xl bg-white p-8 shadow-xl"><h1 className="text-3xl font-bold">Change password</h1><p className="text-sm text-slate-600">You must choose a permanent password before continuing.</p><input name="currentPassword" type="password" required placeholder="Temporary/current password" className="w-full rounded-lg border p-3"/><input name="newPassword" type="password" required placeholder="New password" className="w-full rounded-lg border p-3"/><input name="confirm" type="password" required placeholder="Confirm new password" className="w-full rounded-lg border p-3"/>{message&&<p role="alert" className="text-sm text-red-700">{message}</p>}<button disabled={!csrf} className="w-full rounded-lg bg-emerald-700 p-3 font-semibold text-white">Change password</button></form>}
